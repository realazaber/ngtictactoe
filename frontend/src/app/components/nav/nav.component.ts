import { Component } from '@angular/core';
import { IconsModule } from '../utility/icons/icons.module';

@Component({
  selector: 'app-nav',
  standalone: true,
  imports: [IconsModule],
  templateUrl: './nav.component.html',
  styles: ``,
})
export class NavComponent {}
